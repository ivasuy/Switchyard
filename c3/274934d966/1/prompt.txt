You are implementing Task 2 of the Switchyard Phase 0/1 gap closure plan in the existing repository at /Users/vasuyadav/Downloads/Projects/switchyard. You are not alone in the codebase: do not revert or overwrite changes you did not make; work only in the task-owned files below and adapt to existing code.

User override: do NOT create a separate git worktree. Work in the current checkout only. Commit your completed task changes after tests pass.

Task 2: Add Local Storage Package Skeleton

Owned files:
- Create: packages/storage/package.json
- Create: packages/storage/tsconfig.json
- Create: packages/storage/src/index.ts
- Create: packages/storage/src/sqlite/schema.ts
- Create: packages/storage/src/sqlite/database.ts
- Update pnpm-lock.yaml via pnpm install if dependencies require it

Implementation details:
1. Create packages/storage/package.json exactly matching this package shape:
{
  "name": "@switchyard/storage",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@switchyard/contracts": "workspace:*",
    "@switchyard/core": "workspace:*",
    "better-sqlite3": "^12.5.0",
    "drizzle-orm": "^0.45.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "typescript": "^5.9.3",
    "vitest": "^4.1.5"
  }
}
2. Create packages/storage/tsconfig.json extending ../../tsconfig.base.json with rootDir ".", outDir "dist", include src/**/*.ts and test/**/*.ts.
3. Create packages/storage/src/sqlite/schema.ts with Drizzle sqlite tables: runs, runEvents, runtimeSessions, artifacts. Use imports: integer, sqliteTable, text from drizzle-orm/sqlite-core. Columns must match the plan exactly:
   - runs: id primary key; runtime/provider/model/adapterType/cwd/task/status/placement/approvalPolicy text not null; timeoutSeconds integer not null; metadataJson text not null; createdAt text not null; startedAt/endedAt nullable text. DB column names use snake_case where in plan: adapter_type, approval_policy, timeout_seconds, metadata_json, created_at, started_at, ended_at.
   - runEvents: id primary key; type text not null; runId text run_id not null; debateId, participantId, provider, model nullable text; sequence integer not null; payloadJson text payload_json not null; createdAt text created_at not null.
   - runtimeSessions: id primary key; runId run_id not null; runtime/provider/model/protocol/status text not null; externalSessionKey external_session_key nullable; processId process_id nullable integer; stateJson state_json not null; createdAt created_at not null; updatedAt updated_at nullable.
   - artifacts: id primary key; runId, debateId, provider, model nullable text; type/path text not null; metadataJson metadata_json text not null; createdAt created_at not null.
4. Create packages/storage/src/sqlite/database.ts using better-sqlite3 and drizzle-orm/better-sqlite3. Export type SwitchyardSqliteDatabase, interface OpenSqliteStorageResult { sqlite: Database.Database; db: SwitchyardSqliteDatabase }, function openSqliteStorage(path: string). It should open DB, set journal_mode=WAL, run migrate(sqlite), return drizzle(sqlite, { schema }). Migration SQL should create runs, run_events, runtime_sessions, artifacts plus indexes run_events_run_sequence_idx, runtime_sessions_run_id_idx unique, artifacts_run_id_idx.
5. Create packages/storage/src/index.ts exporting ./sqlite/database.js.
6. Run `pnpm install` from repo root to update pnpm-lock.yaml. If dependency fetching fails due network/sandbox, rerun with required escalation per environment rules.
7. Run `pnpm --filter @switchyard/storage typecheck`.
8. Commit with: git add pnpm-lock.yaml packages/storage && git commit -m "feat: add local sqlite storage package"

Final response format:
STATUS: DONE | DONE_WITH_CONCERNS | NEEDS_CONTEXT | BLOCKED
CHANGED FILES: list paths
TESTS: commands run and pass/fail
COMMIT: commit sha if committed
NOTES: concise self-review and concerns

---

Task 2 quality review found issues that need fixing before Task 3.

Fix these only:

1. HIGH: `better-sqlite3` runtime smoke fails because pnpm ignored native build scripts. Make the package safely runnable in this repo. Preferred fix: add pnpm configuration in the root `package.json` to allow/build `better-sqlite3` (for example pnpm `onlyBuiltDependencies` if appropriate for pnpm 10), run the needed pnpm command to build/install it, and verify a runtime smoke import/open works. Do not switch away from better-sqlite3 unless impossible.

Runtime smoke to run after build:
`pnpm --filter @switchyard/storage build`
then:
`node --input-type=module -e "import { mkdtempSync, rmSync } from 'node:fs'; import { join } from 'node:path'; import { tmpdir } from 'node:os'; import { openSqliteStorage } from './packages/storage/dist/src/sqlite/database.js'; const dir = mkdtempSync(join(tmpdir(), 'switchyard-storage-')); const opened = openSqliteStorage(join(dir, 'test.sqlite')); opened.sqlite.close(); rmSync(dir, { recursive: true, force: true }); console.log('ok');"`
Expected output includes `ok`.

2. MEDIUM: `SwitchyardEvent.runId` is optional, but storage schema/migration currently require `run_events.run_id` NOT NULL. Change `runEvents.runId` and migration `run_id` to nullable text (remove `.notNull()` and `NOT NULL`). Keep index on `(run_id, sequence)`.

Do not address the low unused workspace deps issue; the plan intentionally includes `@switchyard/contracts` and `@switchyard/core` because Task 3 will use them.

Run:
- `pnpm install` or the pnpm build-approval/rebuild command needed for better-sqlite3
- `pnpm --filter @switchyard/storage typecheck`
- `pnpm --filter @switchyard/storage build`
- the runtime smoke command above

Commit with a concise fix message. Final response with STATUS, changed files, tests, commit sha.