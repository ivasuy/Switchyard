You are implementing Task 8 in the current checkout only: /Users/vasuyadav/Downloads/Projects/switchyard. Do NOT create a separate worktree. You are not alone in this codebase; do not revert edits made by others. Current head includes Task 7 through `a77ba23`.

User requested subagent-driven implementation. Own Task 8 end-to-end: add tests, run verification, self-review, and commit before final response.

Task 8: Add Contract Required-Field Negative Coverage

Files:
- Modify: `packages/contracts/test/contracts.test.ts`

Required behavior/spec:
1. Add helper to `packages/contracts/test/contracts.test.ts`:
```ts
import type { z } from "zod";

function expectRequiredFields(schema: z.ZodType, valid: Record<string, unknown>, requiredKeys: string[]): void {
  for (const key of requiredKeys) {
    const value = { ...valid };
    delete value[key];
    expect(() => schema.parse(value), `${key} should be required`).toThrow();
  }
}
```
Adapt imports if the file already imports `z` or uses a different pattern.
2. Add a test named roughly `rejects missing required fields for every public contract schema` using known-valid fixtures and required key lists from the implementation plan. Cover these exported schemas at minimum:
- `runSchema`
- `runtimeSessionSchema`
- `debateSchema`
- `eventSchema`
- `messageSchema`
- `artifactSchema`
- `approvalSchema`
- `memoryItemSchema`
- `evidenceItemSchema`
- `toolInvocationSchema`
- `providerSchema`
- `runtimeSchema`
- `placementDecisionSchema`
- `nodeSchema`
- `userSchema`
- `budgetSchema`
- `contextPacketSchema`
- `errorSchema`

Use the plan fixtures:
- run: id/runtime/provider/model/adapterType/cwd/task/status=queued/placement=local/approvalPolicy=default/timeoutSeconds=60/metadata={}/createdAt. Required: id,runtime,provider,model,adapterType,cwd,task,status,placement,approvalPolicy,timeoutSeconds,createdAt
- runtimeSession: id/runId/runtime/provider/model/protocol=process/status=active/state={}/createdAt. Required: id,runId,runtime,provider,model,protocol,status,createdAt
- debate: id/topic/mode=mixed_model_panel/status=created/participants=[]/limits={ maxRounds:1,maxTurnsPerAgent:1,maxSearchesPerAgent:0,maxTotalMessages:2,maxDurationSeconds:60,maxCostUsd:0,requireCitations:false,requireDisagreementSummary:true,stopOnConsensus:false,stopOnLowNewInformation:false,humanStopAllowed:true }/createdAt. Required: id,topic,mode,status,participants,limits,createdAt
- event: id,type=run.queued,runId,sequence=0,payload={},createdAt. Required: id,type,sequence,payload,createdAt
- message: id,content,deliveryStatus=queued,createdAt. Required: id,content,deliveryStatus,createdAt
- artifact: id,type=transcript,path,metadata={},createdAt. Required: id,type,path,createdAt
- approval: id,approvalType=before_commit,status=pending,payload={},createdAt. Required: id,approvalType,status,payload,createdAt
- memory: id,scope=project,content,metadata={},createdAt. Required: id,scope,content,createdAt
- evidence: id,sourceType=manual,title,reliability=primary,createdAt. Required: id,sourceType,title,reliability,createdAt
- toolInvocation: id,type=repo,status=queued,input={},createdAt. Required: id,type,status,input,createdAt
- provider: id,name,authMode=none,status=available. Required: id,name,authMode,status
- runtime: id,name,adapterType=process,status=available. Required: id,name,adapterType,status
- placementDecision: decision=local,reason,mode=local,requiredCapabilities=[],deniedCapabilities=[],approvalRequired=false,policyTrace=[]. Required: decision,reason,mode,requiredCapabilities,deniedCapabilities,approvalRequired,policyTrace
- node: id,mode=local,status=online,capabilities=[],createdAt. Required: id,mode,status,capabilities,createdAt
- user: id,displayName,createdAt. Required: id,displayName,createdAt
- budget: status=within_budget,maxCostUsd=1,spentCostUsd=0. Required: status,maxCostUsd,spentCostUsd
- contextPacket: id,target=run,sections=[],createdAt. Required: id,target,sections,createdAt
- error: code=validation_failed,message=bad. Required: code,message
3. Run verification:
- `pnpm --filter @switchyard/contracts test`
- Also run contracts typecheck/build if available and cheap.
4. Commit exactly:
`test: add contract required field coverage`

Final response format:
- Status: DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
- Files changed
- Tests run and results
- Commit SHA
- Self-review notes.

---

Quality review returned CHANGES_REQUESTED for Task 8. Please fix in the current checkout only, run verification, and commit a follow-up fix. Do not revert unrelated work.

Findings to address:
1. Add required-field coverage for public `modelSchema` from registry contracts. Required keys: likely `id`, `providerId`, `modelName`, `status`; inspect current schema to confirm and use a valid fixture.
2. Add required-field coverage for public `contextSectionSchema`. Required keys: likely `name`, `content`; inspect current schema to confirm and use a valid fixture.
3. Strengthen `expectRequiredFields` so it first asserts the baseline `valid` fixture parses successfully before deleting keys. This prevents false positives if a fixture drifts invalid. Keep the helper simple.

Run:
- `pnpm --filter @switchyard/contracts test`
- `pnpm --filter @switchyard/contracts typecheck`
- `pnpm --filter @switchyard/contracts build`

Commit follow-up with a clear message, e.g. `fix: complete contract required field coverage`.

Final response format:
- Status
- Files changed
- Tests run and results
- Commit SHA
- Notes