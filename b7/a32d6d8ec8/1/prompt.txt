You are implementing Task 13 in the current checkout only: /Users/vasuyadav/Downloads/Projects/switchyard. Do NOT create a separate worktree. You are not alone in this codebase; do not revert edits made by others. Current head includes Task 12 through `1e92e17`.

User requested subagent-driven implementation. Own Task 13 end-to-end: add tests first where practical, implement, run verification, self-review, and commit before final response.

Task 13: Persist Artifact Content End-to-End

Files:
- Modify: `packages/core/src/services/runtime-runner-service.ts`
- Modify: `packages/testkit/src/fake-runtime-adapter.ts`
- Modify: `packages/storage/src/filesystem-artifact-content-store.ts`
- Modify: `apps/daemon/src/app.ts`
- Modify: `apps/daemon/test/smoke.test.ts`

Required behavior/spec:
1. Update fake runtime adapter `artifacts()` return so artifact metadata includes content:
```ts
metadata: { content: "{\"type\":\"runtime.output\",\"text\":\"fake runtime output\"}\n" },
```
Adapt to current returned artifact shape while preserving existing metadata if needed.
2. Add optional artifact content dependency to `RuntimeRunnerDependencies` in `packages/core/src/services/runtime-runner-service.ts`:
```ts
artifactContent?: { writeText(path: string, content: string): Promise<string> };
```
When storing each adapter artifact:
```ts
const content = typeof artifact.metadata["content"] === "string" ? artifact.metadata["content"] : undefined;
const path = content && this.deps.artifactContent ? await this.deps.artifactContent.writeText(artifact.path, content) : artifact.path;
const storedArtifact = await this.deps.artifacts.create({
  ...artifact,
  path,
  metadata: { ...artifact.metadata, contentStored: Boolean(content) },
  runId: started.id,
  provider: artifact.provider ?? started.provider,
  model: artifact.model ?? started.model
});
```
Adapt to current id/createdAt normalization from Task 5, and do not break artifact.created payload path/type. Be careful to avoid leaving raw content in persisted metadata if that is a security/storage concern; spec says include contentStored but doesn't explicitly say remove content. Use judgment and tests.
3. Wire daemon content store:
- In `apps/daemon/src/app.ts`, when config is provided, pass `artifactContent: new FilesystemArtifactContentStore(config.artifactDir)` to `RuntimeRunnerService`.
- Ensure in-memory/no-config mode does not require filesystem content storage.
- If `FilesystemArtifactContentStore` import was removed in Task 7 cleanup, re-add it.
4. `packages/storage/src/filesystem-artifact-content-store.ts` may need adjustment so `writeText(path, content)` returns a path suitable for later `join(config.artifactDir, artifact.path)` reads. Preserve prior path traversal protections and tests from Task 4.
5. Update daemon persistence smoke test to verify file content:
```ts
const artifact = artifacts.json().artifacts[0];
expect(readFileSync(join(config.artifactDir, artifact.path), "utf8")).toContain("fake runtime output");
```
Add/import `readFileSync` if needed. This should run after reopened app fetches artifacts.
6. Run verification:
- `pnpm --filter @switchyard/core test`
- `pnpm --filter @switchyard/storage test`
- `pnpm --filter @switchyard/daemon test`
- Also run core/storage/daemon typecheck/build if cheap.
7. Commit exactly:
`feat: persist fake runtime artifact content`

Final response format:
- Status: DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
- Files changed
- Tests run and results
- Commit SHA
- Self-review notes, including any intentional deviations from plan.

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.