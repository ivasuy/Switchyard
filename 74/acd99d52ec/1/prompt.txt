Task 2 quality re-review found one remaining issue to fix.

Issue:
- `packages/storage/package.json` has `test: vitest run`, but there are no test files yet, so `pnpm --filter @switchyard/storage test` and root `pnpm test` fail.

Fix:
1. Add a minimal storage skeleton smoke test in `packages/storage/test/storage-package.test.ts` that imports `openSqliteStorage`, opens an in-memory SQLite DB, verifies it can run a simple query or returns an object with `sqlite` and `db`, closes it. This keeps the package test script green until Task 3 adds persistence tests.
2. Run:
   - `pnpm --filter @switchyard/storage test`
   - `pnpm --filter @switchyard/storage typecheck`
   - `pnpm test` (root) if feasible; if it is slow, run it and report result.
3. Commit with a concise fix message.

Do not alter package scripts to skip tests. Do not touch unrelated files.