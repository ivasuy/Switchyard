{
  "name": "@switchyard/protocol-rest",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm --filter @switchyard/protocol-rest... build && vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@switchyard/contracts": "workspace:*",
    "@switchyard/core": "workspace:*",
    "@switchyard/protocol-sse": "workspace:*",
    "fastify": "^5.8.5",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@switchyard/testkit": "workspace:*"
  }
}
