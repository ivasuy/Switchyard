Task 3 quality review found issues that need fixing before we move on.

Fix these issues:

1. MEDIUM: Update methods leave stale nullable values because `toRow(...)` omits optional nullable fields. For update paths, optional nullable columns must be explicitly set to null when absent so values can be cleared.
   - Affected: run-store (`startedAt`, `endedAt`), session-store (`externalSessionKey`, `processId`, `updatedAt`), artifact-store (`runId`, `debateId`, `provider`, `model`).
   - Suggested approach: either make `toRow` always set nullable fields to `value ?? null`, or have a separate `toUpdateRow` that explicitly nulls nullable fields. Keep exactOptionalPropertyTypes-compatible returned contract objects.

2. MEDIUM: Expand tests in `packages/storage/test/sqlite-storage.test.ts`:
   - verify `update(...)` can clear optional fields for runs/sessions/artifacts.
   - verify `SqliteEventStore.listByRun` orders by `sequence` when multiple events exist and filters out other run ids.
   - verify `SqliteArtifactStore.listByRun` filters by run id when multiple artifacts exist.
   Follow TDD: add/adjust tests, run `pnpm --filter @switchyard/storage test` and observe failure before implementation if possible.

3. LOW but easy: Pull forward the core port change: update `packages/core/src/ports/artifact-store.ts` so `ArtifactStore` is an interface extending `GenericStore<Artifact>` with `listByRun(runId: string): Promise<Artifact[]>`. This matches the concrete sqlite implementation and future tasks. Run affected tests/typecheck.

Accepted/deferred: Do NOT add JSON.parse corruption handling right now; this is deferred because rows are written by our own stores and later validation/error policy can handle corrupted DB rows.

Run:
- `pnpm --filter @switchyard/storage test`
- `pnpm --filter @switchyard/storage typecheck`
- `pnpm --filter @switchyard/storage build`
- `pnpm --filter @switchyard/core typecheck`
- `pnpm test`

Commit with a concise fix message. Do not touch unrelated files.