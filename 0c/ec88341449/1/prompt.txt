Quality re-review found one remaining Task 11 issue. Please fix in current checkout only, run verification, and commit a small follow-up.

Issue:
- `stopAfter` semantics diverge between live and non-live paths for fractional values in `(0,1)`. REST floors `0.5` to `0`, so non-live `slice(0, 0)` returns empty, but protocol-sse helper treats `<= 0` as full-replay fallback, so live returns full replay.

Fix:
- Normalize `stopAfter` consistently before both live and non-live paths. Preferred behavior: invalid, non-finite, or non-positive should default to replay length in both route and helper. Fractional positive values should either floor then if <1 default to replay length, or clamp to at least 1; choose one and test it consistently. Keep current documented safe behavior if possible: non-positive/invalid defaults to replay length.
- Add tests covering `stopAfter=0.5` for protocol-rest live and non-live parity, and protocol-sse helper behavior for fractional/non-positive values.

Run:
- `pnpm --filter @switchyard/protocol-sse test`
- `pnpm --filter @switchyard/protocol-rest test`
- `pnpm --filter @switchyard/protocol-sse typecheck`
- `pnpm --filter @switchyard/protocol-rest typecheck`
- `pnpm --filter @switchyard/protocol-sse build`
- `pnpm --filter @switchyard/protocol-rest build`

Commit follow-up with a clear message, e.g. `fix: normalize fractional sse stopAfter`.

Final response: status, files changed, tests, commit SHA, notes.