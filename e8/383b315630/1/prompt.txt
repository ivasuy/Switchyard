Quality review returned CHANGES_REQUESTED for Task 6. Please fix the route-level issues in the current checkout only, run verification, and commit a follow-up fix commit. Do not touch daemon files in this task; daemon wiring/smoke updates are Task 7 scope. Do not revert unrelated work.

Findings to address now:
1. High route-level issue: `POST /runs` async default can create permanently queued runs if `deps.launcher` is omitted. Preserve the Task 6 contract that default POST returns 202, but ensure the run still starts. A fallback microtask that calls `deps.runService.startRun(run.id)` if no launcher is injected is acceptable. Add a test where `launcher` is omitted and verify `startRun` is invoked/asynchronous path is not stuck.
2. Medium: wait query parsing is brittle. Add a small helper so `wait=1` works, repeated query values including `1` work, and non-1 values remain async. Keep semantics conservative; do not broaden to wait=true unless you intentionally test/document it.
3. Medium/test gap: missing artifact store behavior is spec’d as `{ artifacts: [] }`; add a route test proving that behavior for an existing run when `deps.artifacts` is undefined.

Notes:
- It is okay that `apps/daemon` smoke currently expects old sync status; Task 7 is explicitly daemon wiring and smoke update scope. Do not edit daemon in Task 6.
- Keep `formatSseEvent` behavior unchanged.

Run:
- `pnpm --filter @switchyard/protocol-rest test`
- `pnpm --filter @switchyard/protocol-rest typecheck`
- `pnpm --filter @switchyard/protocol-rest build`

Commit follow-up with a clear message, e.g. `fix: harden async run route fallback`.

Final response format:
- Status: DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
- Files changed
- Tests run and results
- Commit SHA
- Notes on how each review finding was addressed