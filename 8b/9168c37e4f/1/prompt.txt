You are implementing Task 14 in the current checkout only: /Users/vasuyadav/Downloads/Projects/switchyard. Do NOT create a separate worktree. You are not alone in this codebase; do not revert edits made by others. Current head includes Task 13 through `353a52d`.

User requested subagent-driven implementation. Own Task 14 end-to-end: update docs, run verification, self-review, and commit before final response.

Task 14: Update Docs and Verify

Files:
- Modify: `README.md`
- Modify: `CHANGELOG.md`
- Modify: `docs/superpowers/plans/2026-05-11-switchyard-master-implementation-plan.md`

Required behavior/spec:
1. Update README local testing section to state:
```markdown
The current local MVP runs a fake runtime through the Switchyard daemon. It uses local SQLite state and filesystem artifact metadata by default. It does not call Claude, Codex, OpenCode, or any external model yet.
```
Add artifact check command:
```bash
curl -s http://127.0.0.1:4545/runs/<RUN_ID>/artifacts
```
Use nearby existing style and avoid over-editing unrelated README sections.
2. Update CHANGELOG under `0.1.0` with bullets:
```markdown
- Added local SQLite-backed run, event, session, and artifact stores.
- Added local SQLite-backed message, approval, registry, and placement-decision stores.
- Added filesystem artifact content storage for local daemon mode.
- Added `GET /runs/:id/artifacts`.
- Added registry lookup routes for providers, runtimes, and models.
- Added `@switchyard/protocol-sse` for replay and live run event streams.
- Added async run launch support for local daemon execution.
- Added required-field negative contract coverage for public schemas.
```
Adapt placement under existing 0.1.0 section without duplicating existing bullets unnecessarily.
3. In `docs/superpowers/plans/2026-05-11-switchyard-master-implementation-plan.md`, add short status note under Phase 1:
```markdown
Status note, 2026-05-11: Phase 1 is complete when the storage-backed fake daemon, artifact listing, and replay/live event behavior in the Phase 0/1 gap plan are merged and verified.
```
4. Run full verification:
```bash
pnpm test
pnpm typecheck
pnpm build
pnpm lint
```
If one of these scripts is missing or fails due to project-wide script setup, report clearly and run the closest package-level equivalent if needed. Do not skip commands silently.
5. Commit exactly:
`docs: update phase 1 local mvp status`

Final response format:
- Status: DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
- Files changed
- Verification commands run and results
- Commit SHA
- Self-review notes.