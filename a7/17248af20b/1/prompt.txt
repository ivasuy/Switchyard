Quality re-review found one real bug. Please fix in current checkout only, run verification, and commit a small follow-up.

Issue:
- `expect(() => schema.parse(valid)).not.toThrow("fixture should parse")` is wrong: the string is a matcher argument, not an assertion message. It does not guarantee the baseline fixture parses.

Fix to either:
```ts
expect(() => schema.parse(valid), "fixture should parse").not.toThrow();
```
or simply:
```ts
expect(() => schema.parse(valid)).not.toThrow();
```
Prefer the first if Vitest supports assertion messages there.

Run:
- `pnpm --filter @switchyard/contracts test`
- `pnpm --filter @switchyard/contracts typecheck`
- `pnpm --filter @switchyard/contracts build`

Commit follow-up with a clear message, e.g. `fix: validate contract fixture baselines`.

Final response format:
- Status
- Files changed
- Tests run and results
- Commit SHA
- Notes