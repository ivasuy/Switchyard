Task 1 quality review found a medium issue that needs fixing before we move on.

Fix only this issue:
- `packages/core` now depends on `@switchyard/testkit`, but `@switchyard/testkit` depends on `@switchyard/core`, creating a dev/test package cycle.

Required fix:
1. Remove `@switchyard/testkit` from `packages/core/package.json` devDependencies and update `pnpm-lock.yaml` accordingly.
2. Keep `packages/core/test/runtime-adapter-contract.test.ts`, but make it self-contained by defining a tiny local `ContractAdapter` class in the test file that implements/imports `RuntimeAdapter` from `../src/index.js` (or otherwise satisfies the adapter shape) and emits the same runtime.status/runtime.output/run.completed events, fake.echo tool, and transcript artifact. Do not import from `@switchyard/testkit` in core tests.
3. Preserve the task’s contract coverage: check, start, events, send, cancel, tools, artifacts, sessionId /^session_/, runId `run_contract`, event types, tools, artifact type.
4. Run:
   - `pnpm --filter @switchyard/core test`
   - `pnpm --filter @switchyard/core typecheck`
   - `pnpm --filter @switchyard/core build`
5. Commit with a concise fix message.

Do not address the low issues; those are accepted as intentional plan constraints for now. Do not touch unrelated files.