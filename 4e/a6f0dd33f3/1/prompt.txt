Quality review returned CHANGES_REQUESTED for Task 9. Please fix in the current checkout only, run verification, and commit a follow-up.

Findings to address:
1. `PlacementDecisionRecord.runId` is optional in the written Task 9 spec, so do NOT change the port type unless absolutely necessary. Instead, enforce runtime safety in `InMemoryPlacementStore`: reject `create`/`update` records without `runId` (throw a clear error). This preserves the spec shape while preventing unqueryable placement records in the testkit store.
2. Strengthen `fake-runtime-adapter.test.ts` for the new stores:
   - create artifacts and placements across two run IDs;
   - assert `listByRun("run_123")` excludes non-matching run records;
   - assert update behavior for both stores keeps filtering stable (e.g. update an existing record’s metadata/reason or runId and verify get/list results).

Run:
- `pnpm --filter @switchyard/core test`
- `pnpm --filter @switchyard/testkit test`
- `pnpm --filter @switchyard/core typecheck`
- `pnpm --filter @switchyard/testkit typecheck`
- `pnpm --filter @switchyard/core build`
- `pnpm --filter @switchyard/testkit build`

Commit follow-up with a clear message, e.g. `fix: harden in-memory placement store coverage`.

Final response format:
- Status
- Files changed
- Tests run and results
- Commit SHA
- Notes

---

Quality re-review approved, but noted one cheap residual test gap. Please add a small follow-up test assertion in the current checkout only and commit it:

- In `packages/testkit/test/fake-runtime-adapter.test.ts`, add a mirror assertion that `InMemoryPlacementStore.update(...)` rejects a record missing `runId`, matching the existing `create` negative test and the runtime guard in `update`.

Run:
- `pnpm --filter @switchyard/testkit test`
- `pnpm --filter @switchyard/testkit typecheck`
- `pnpm --filter @switchyard/testkit build`

Commit follow-up with message like `test: cover placement update run id guard`.

Final response: status, files changed, tests, commit SHA.