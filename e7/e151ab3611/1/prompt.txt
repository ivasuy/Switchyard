Quality review returned CHANGES_REQUESTED for Task 5. Please fix the concrete issues below in the current checkout only, run verification, and commit a follow-up fix commit. Do not revert unrelated work.

Findings to address:
1. High: Event ordering can expose stale run/session state to eventBus consumers. In `RuntimeRunnerService.start`, terminal events are appended/published before terminal run/session records are updated. Adjust so persisted run/session state is updated before publishing terminal `run.completed` / `run.failed` events. Keep event sequence correct.
2. High: Error path leaves inconsistent terminal state. Add handling around adapter startup/stream/artifact collection so adapter failures produce a persisted terminal failed run/session state and a `run.failed` event instead of bubbling without cleanup. Preserve useful error info in event data if project patterns allow.
3. Medium: `RunLauncherService.launch` drops rejected `startRun` promises. Add a conservative error handling strategy that prevents unhandled rejections without changing the public API. If no logger exists, attach a catch and document why minimally.
4. Medium: Cancellation can race with completion + artifact collection. Add a guard so if the run has been cancelled, ongoing stream completion/artifact collection cannot overwrite cancellation or publish artifacts after cancellation.
5. Medium: Duplicate artifact IDs with `artifact_` prefix can collide. Make stored artifact IDs unique per persisted artifact, while still preserving existing behavior where sensible. A deterministic prefix plus random suffix is acceptable.
6. Medium: EventBus failures can fail runtime execution after event persistence. Decide and implement a resilient behavior for optional event bus publishing so one subscriber failure does not corrupt the run path. Keep this scoped.

Also strengthen tests enough to cover the important fixes: at least terminal event publish-after-state update, adapter failure terminalization, launcher rejection handling or no-unhandled rejection behavior if practical, cancellation/artifact guard if feasible, duplicate artifact id handling, and eventBus subscriber failure isolation.

Run:
- `pnpm --filter @switchyard/core test`
- `pnpm --filter @switchyard/core typecheck`
- `pnpm --filter @switchyard/core build`

Commit follow-up with a clear message, e.g. `fix: harden runtime artifact event flow`.

Final response format:
- Status: DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
- Files changed
- Tests run and results
- Commit SHA
- Notes on how each review finding was addressed

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.

---

Warning: apply_patch was requested via exec_command. Use the apply_patch tool instead of exec_command.