Quality review returned CHANGES_REQUESTED for Task 10. Please fix in the current checkout only, run verification, and commit a follow-up.

Required fix:
1. Align `SqlitePlacementStore` with the in-memory placement store invariant: reject create/update records without `runId` using a clear error message, e.g. `placement records require a runId`. Keep the schema/migration column nullable if needed to preserve the written Task 10 DB shape; enforce at store boundary.
2. Add storage tests for:
   - `SqlitePlacementStore.create` rejects missing `runId`;
   - `SqlitePlacementStore.update` rejects missing `runId`;
   - `SqlitePlacementStore.listByRun` filters out other-run records.
3. If practical, add one compact update/clear test for new stores that verifies null-clearing behavior for optional fields on `SqliteMessageStore` and `SqliteApprovalStore`, and update behavior for `SqlitePlacementStore` remains queryable by run after update. This addresses residual risks but keep it scoped.

Run:
- `pnpm --filter @switchyard/storage test`
- `pnpm --filter @switchyard/storage typecheck`
- `pnpm --filter @switchyard/storage build`

Commit follow-up with a clear message, e.g. `fix: harden sqlite placement store invariants`.

Final response format:
- Status
- Files changed
- Tests run and results
- Commit SHA
- Notes