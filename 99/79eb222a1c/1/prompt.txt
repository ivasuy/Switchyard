You are implementing Task 12 in the current checkout only: /Users/vasuyadav/Downloads/Projects/switchyard. Do NOT create a separate worktree. You are not alone in this codebase; do not revert edits made by others. Current head includes Task 11 through `1aad055`.

User requested subagent-driven implementation. Own Task 12 end-to-end: add tests first where practical, implement, run verification, self-review, and commit before final response.

Task 12: Add Registry REST Routes

Files:
- Create: `packages/protocol-rest/src/registry-routes.ts`
- Modify: `packages/protocol-rest/src/index.ts`
- Create: `packages/protocol-rest/test/registry-routes.test.ts`
- Modify: `apps/daemon/src/app.ts`
- Likely also modify `apps/daemon/src/main.ts` and `apps/daemon/test/smoke.test.ts` if `createDaemonApp` becomes async per the plan.

Required behavior/spec:
1. Add `packages/protocol-rest/src/registry-routes.ts` with `registerRegistryRoutes(app, { registry })` exposing:
- `GET /providers/:id` returning `{ provider }` or 404 `{ error: { code: "provider_not_found", message: `Provider not found: ${id}` } }`
- `GET /runtimes/:id` returning `{ runtime }` or 404 `{ error: { code: "runtime_not_found", message: `Runtime not found: ${id}` } }`
- `GET /models/:id` returning `{ model }` or 404 `{ error: { code: "model_not_found", message: `Model not found: ${id}` } }`
Use `FastifyInstance` and `RegistryStore` types.
2. Export from `packages/protocol-rest/src/index.ts`:
```ts
export * from "./registry-routes.js";
```
3. Add `packages/protocol-rest/test/registry-routes.test.ts` with a `MemoryRegistryStore` implementing `RegistryStore`. Test successful GETs for provider/runtime/model. Add 404 tests if useful.
4. Wire daemon registry:
- In `apps/daemon/src/app.ts`, include `registry: new SqliteRegistryStore(db)` in `createStorageStores(config)`.
- Ensure no-config/in-memory mode also provides a registry store. If no testkit registry store exists, add a local simple in-memory registry implementation in daemon app.
- Change `createDaemonApp` to `async function createDaemonApp(config?: DaemonConfig)` if needed to seed registry records before route registration. Update daemon tests and `main.ts` to `await createDaemonApp(...)`.
- After creating stores, seed fake registry if records are absent:
```ts
if (!(await stores.registry.getProvider("provider_test"))) {
  await stores.registry.createProvider({ id: "provider_test", name: "Test Provider", authMode: "none", status: "available" });
}
if (!(await stores.registry.getRuntime("runtime_fake"))) {
  await stores.registry.createRuntime({ id: "runtime_fake", name: "Fake Runtime", adapterType: "process", status: "available" });
}
if (!(await stores.registry.getModel("model_test"))) {
  await stores.registry.createModel({ id: "model_test", providerId: "provider_test", modelName: "test-model", supportsTools: false, supportsStreaming: true, supportsBrowser: false, status: "available" });
}
```
- Register routes with:
```ts
registerRegistryRoutes(app, { registry: stores.registry });
```
5. Run verification:
- `pnpm --filter @switchyard/protocol-rest test`
- `pnpm --filter @switchyard/daemon test`
- Also run protocol-rest/daemon typecheck/build if available and cheap.
6. Commit exactly:
`feat: add registry rest routes`

Final response format:
- Status: DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED
- Files changed
- Tests run and results
- Commit SHA
- Self-review notes, including any intentional deviations from plan.