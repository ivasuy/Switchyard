Task 4 quality re-review found one high issue to fix.

Issue:
- `safePath()` does not reject Windows drive-relative / drive-qualified logical paths like `D:foo`, `C:..`, and UNC-style paths. Even though current environment is macOS/POSIX, this storage package should be cross-platform-safe.

Fix:
1. Reject logical paths that are Windows drive-qualified or UNC-style before resolving/writing. Examples to reject with `Artifact path escapes root`:
   - `D:foo`
   - `C:..`
   - `C:/escape.txt` or `C:\escape.txt`
   - `//server/share/file.txt` and `\\server\share\file.txt`
2. Keep existing behavior:
   - accept `runs/a..b.txt`
   - reject backslashes generally if that remains your approach
   - reject POSIX absolute paths and `../` traversal
3. Add tests for drive-relative/drive-qualified/UNC-style rejection. Since this runs on macOS too, tests should assert the input is rejected by our string checks, not by platform-specific path behavior.
4. Run:
   - `pnpm --filter @switchyard/storage test`
   - `pnpm --filter @switchyard/storage typecheck`
   - `pnpm --filter @switchyard/storage build`
   - `pnpm test`
   - `pnpm typecheck`
   - `pnpm build`
5. Commit with a concise fix message.

Do not attempt symlink resolution in this task; symlink-safe storage can be a later hardening item if needed. Do not touch unrelated files.