Final review approved Task 13 but surfaced one residual risk worth fixing before we move on: artifact-created events and terminal events may share duplicate `sequence` values after the artifact-before-terminal refactor.

Please fix in the current checkout only, run verification, and commit a small follow-up:
- Ensure every event appended by `RuntimeRunnerService` for a run gets a unique monotonically increasing sequence, including adapter stream events, artifact.created events, and terminal run.completed/run.failed events after artifact persistence.
- Add/adjust a core test that asserts sequence uniqueness/order for a successful run with artifacts where artifact.created precedes run.completed.
- Preserve the Task 13 behavior: artifact rows/events are only committed after all artifacts are prepared; persistence failure still yields failed run with no artifact rows/events.

Run:
- `pnpm --filter @switchyard/core test`
- `pnpm --filter @switchyard/core typecheck`
- `pnpm --filter @switchyard/core build`

Commit follow-up with message like `fix: preserve runtime event sequence order`.

Final response: status, files changed, tests, commit SHA, notes.