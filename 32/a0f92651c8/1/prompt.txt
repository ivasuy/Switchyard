Quality review returned CHANGES_REQUESTED for Task 12. Please fix in current checkout only, run verification, and commit a follow-up.

Findings to address:
1. Bootstrap failure cleanup: `createDaemonApp` opens SQLite and then awaits `seedFakeRegistry`; if seeding throws, the sqlite handle should be closed. Add a try/catch around async bootstrap after stores are created so `stores.close?.()` or equivalent runs before rethrow. Preserve normal `app.onClose` behavior for successful creation.
2. Extend daemon smoke persistence test after reopen to exercise seeded registry routes:
   - `GET /providers/provider_test` returns provider name `Test Provider`
   - `GET /runtimes/runtime_fake` returns runtime name `Fake Runtime`
   - `GET /models/model_test` returns modelName `test-model`
This proves seeded registry persistence/idempotency through daemon route wiring.

Run:
- `pnpm --filter @switchyard/protocol-rest test`
- `pnpm --filter @switchyard/daemon test`
- `pnpm --filter @switchyard/protocol-rest typecheck`
- `pnpm --filter @switchyard/daemon typecheck`
- `pnpm --filter @switchyard/protocol-rest build`
- `pnpm --filter @switchyard/daemon build`

Commit follow-up with a clear message, e.g. `fix: harden daemon registry bootstrap`.

Final response: status, files changed, tests, commit SHA, notes.